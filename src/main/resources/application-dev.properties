# Configurações do datasource MySQL Railway
spring.datasource.url=jdbc:mysql://interchange.proxy.rlwy.net:10699/railway
spring.datasource.username=root
spring.datasource.password=nyTcIUgkPDnTpBJZrIiNgXQFpmReAxDJ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Opções recomendadas para conexões MySQL
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# Dialeto do Hibernate para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Mostra SQL no console (opcional)
spring.jpa.show-sql=true

# Atualiza o esquema automaticamente (ajuste conforme necessário)
spring.jpa.hibernate.ddl-auto=create
